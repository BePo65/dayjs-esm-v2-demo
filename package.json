{
  "name": "test-dayjs-typescript-esm-updated",
  "version": "1.0.0",
  "description": "A project to test dayjs using TypeScript.",
  "keywords": [
    "typescript",
    "dayjs"
  ],
  "author": {
    "name": "BePo65"
  },
  "license": "MIT",
  "main": "index.ts",
  "type": "module",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "npm run clean && tsc --project tsconfig.app.json",
    "start": "npm run build && node --experimental-specifier-resolution=node dist/index.js",
    "test": "cross-env NODE_OPTIONS=\"--experimental-vm-modules\" jest",
    "clean": "cf dist",
    "upgrade": "npx npm-check -u"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^16.0.1",
    "clear-folder": "^3.0.0",
    "cross-env": "^7.0.3",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.3",
    "ts-node": "^10.0.0",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "dayjs": "^1.10.6"
  }
}
